#import Repo from './Repo.graphql'

type User {
  bio: String
  followers: Int
  following: Int
  name: String
  pic: String
  profileCreatedAt: String
  repos: [Repo]
  status: Int
  uid: String
  url: String
  username: String!
}
