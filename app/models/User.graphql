#import PullRequest from './PullRequest.graphql'
#import Repo from './Repo.graphql'

type User {
  bio: String
  followers: Int
  following: Int
  issues: Int
  name: String
  pic: String
  profileCreatedAt: String
  pullRequests: [PullRequest]
  repos: [Repo]
  status: Int
  time: String
  uid: String
  url: String
  username: String!
}
